export interface iFilm {
  Title: string;
  Year: string;
  Poster: string;
  imdbID: string;
  Plot?: string;
  Fav?: boolean;
  // constructor(public Title:string, public Year:string, public Poster:string){}
}
